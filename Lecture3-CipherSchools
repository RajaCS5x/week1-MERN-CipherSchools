GIT Command Class-3

git add . (also add Modified changes)

//checkout
git checkout TRACK_ID (from git log --online) tell use how project was at that commit now <git log> it show only upto that particular commit only.... 😮 to go back use git checkout master

NOTE: If you want to change history you can but not in same branch

--------------------------------------------------------------------

GIT COMMAND Class-4

git revert  (revert to some point of time OPEN VI editor)

git reset TRACK_ID --hard/--soft FLAGS 

git checkout -b BRANCH_NAME (create and take to that branch)

git checkout BRANCH_NAME (take to that branch)